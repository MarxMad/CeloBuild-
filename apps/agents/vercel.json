{
  "buildCommand": "pip install -r requirements.txt",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ],
  "crons": [
    {
      "path": "/api/lootbox/scan",
      "schedule": "*/30 * * * *"
    }
  ]
}
